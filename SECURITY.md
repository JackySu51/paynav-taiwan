# SECURITY.md

> **這份文件是活的，不是寫一次交差。** 每次加新功能（尤其是任何會存取使用者資料、
> 接後端服務、加登入的功能），這份文件要跟著更新。維護規則見文件最下方。

---

## 這個 App 目前實際在保護什麼

老實列出來，不誇大也不隱藏：

| 資料 | 敏感度 | 目前存在哪 |
|---|---|---|
| 持有哪些信用卡 | 中——不是機密，但是個人消費輪廓的一部分 | 使用者裝置本機（localStorage） |
| 自訂卡片與回饋規則 | 低 | 使用者裝置本機 |
| 條碼／會員卡數值 | 中——可能包含真實的會員卡號、自然人憑證條碼 | 使用者裝置本機 |
| 本月記帳／回饋上限使用量 | 低 | 使用者裝置本機 |
| GPS 座標 | 中——只在當下用來比對門市，不儲存、不上傳 | 只存在記憶體，關閉頁面即消失 |

**現在（尚未接後端）：所有資料只在使用者自己的裝置上，沒有伺服器，沒有第三方能看到。** 這不只是資安考量，也是刻意的產品設計——不收集的資料，永遠不會外洩。

**沒有存的東西**：完整信用卡卡號、身分證字號、真實姓名、地址、任何金流資訊。這個 App 不處理付款，只做「推薦你用哪個工具」，這件事本身就限制了資料外洩能造成的最大傷害。

---

## 威脅模型：我們認真防的是什麼，不假裝防的是什麼

**誠實的立場：沒有任何連網系統對「有充分資源與時間的頂尖攻擊者」是絕對安全的。** 這份文件的目標不是宣稱不可能被攻破，而是：

1. 消除所有「自動化掃描工具幾小時內就能找到」的常見漏洞（暴露的金鑰、缺少的權限檢查、SQL Injection 這類）
2. 讓資安投入的程度跟資料的實際敏感度成正比——這個 App 不是銀行，不用防「國家級攻擊者針對性入侵」這種不對稱的目標
3. 如果真的有人發現問題，提供清楚的回報管道（見下方），讓善意的資安研究者可以負責任地告訴我們，而不是被迫公開揭露或直接利用

---

## 接了後端服務之後的架構（規劃中，尚未實作）

一旦加入 Turso（資料庫）與 Supabase Auth（登入），架構會是：

```
使用者裝置（App / 網頁）
    │
    │  只帶「登入後拿到的 session token」
    ▼
Cloudflare Worker（唯一能碰資料庫的地方）
    │  1. 驗證 token 是否有效
    │  2. 確認這個使用者只能存取自己的資料（程式碼裡明確檢查 user_id）
    │  3. 用參數化查詢（不是字串拼接）跟 Turso 溝通
    ▼
Turso（資料庫，只存皮夾同步用的 JSON，不存密碼）
```

**關鍵決定，以及為什麼這樣做：**

- **使用者裝置永遠不直接連 Turso。** 資料庫的存取憑證只存在 Cloudflare Worker 的環境變數裡，前端程式碼裡完全不會出現。這跟「前端直接拿公開金鑰查資料庫、靠資料庫政策擋權限」的做法不同——那種做法一旦資料庫政策設錯，就直接被繞過（這是 2026 年多起 AI 生成應用資安事件的根因）。我們的做法是「前端連都連不到資料庫」，攻擊面本身就小很多。
- **Turso 沒有內建的 Row Level Security，所以「使用者只能看自己的資料」這件事，明確寫在 Worker 的程式碼裡**，不是寫在資料庫設定裡。程式碼可以被逐行檢查、寫測試驗證；資料庫政策設定容易漏設一條就整組失效。
- **密碼與登入，交給 Supabase Auth 處理，不自己刻。** 登入系統是最容易出錯、也最常被攻擊的一塊，用一個成熟、被大量正式產品驗證過的元件，比自己寫安全。
- **這一層的程式碼由人工審查過才會上線**，不會直接套用 AI 工具生成後未經檢查的版本——尤其是授權檢查的邏輯，這是資安關鍵路徑，值得多花時間看過。

---

## 具體檢查清單（每次加後端功能前，逐項確認）

- [ ] 有沒有任何 API 金鑰、資料庫密碼、Token 寫死在程式碼或推上 GitHub？（用 `.env`，並確認 `.gitignore` 排除它）
- [ ] GitHub 的 Secret Scanning 有沒有跳出警告？
- [ ] 資料庫查詢是不是都用參數化查詢（`?` 佔位符），而不是把使用者輸入直接拼進 SQL 字串？
- [ ] 每一個會讀寫使用者資料的 API，是否都先確認「這個 token 對應的使用者，是不是這筆資料的擁有者」？
- [ ] 是否對登入嘗試、API 呼叫頻率做了基本的速率限制（避免暴力破解或濫用）？
- [ ] 錯誤訊息會不會洩漏內部細節（資料庫結構、伺服器路徑）？應該回傳籠統的錯誤給使用者，詳細內容只寫進伺服器端的 log
- [ ] 第三方套件是否有已知漏洞？（`npm audit`，或 GitHub 的 Dependabot 提醒）
- [ ] 使用者可以刪除自己的帳號與所有資料嗎？（這是基本的資料自主權，不只是資安）

---

## 資安問題回報

如果你發現這個專案有資安疑慮，麻煩透過以下方式回報，**不要公開發 issue**（公開發 issue 等於告訴所有人這個洞在哪，在我們來得及修之前）：

- 私訊 GitHub 帳號 [@JackySu51](https://github.com/JackySu51)
- 或寄信到（尚未設立專用信箱前，暫用個人聯絡方式）

回報時盡量附上：怎麼重現、可能的影響範圍、你的聯絡方式（方便我們回報進度）。我們會盡快確認並修復，修復後會在這份文件的「更新紀錄」感謝你（除非你希望匿名）。

---

## 已知限制（誠實列出，不要略過不寫）

- 目前（尚未接後端）：無登入系統，換裝置無法同步資料——這是刻意的設計，不是漏洞
- 尚未實作：Turso／Supabase Auth 串接、Cloudflare Worker 中介層（見上方規劃）
- 尚未做：正式的第三方滲透測試。等使用者規模成長到有實質資料量時，會考慮找專業資安服務做一次審查

---

## 更新紀錄

| 日期 | 變動 |
|---|---|
| 2026-08 | 建立文件，記錄目前 local-first 架構與未來 Turso/Supabase Auth 的規劃 |

---

## 維護規則（給之後的我們自己看）

這份文件跟著 `.env.example`、`README.md` 的「架構與安全性」小節一起維護，規則：

| 加的東西 | 要同步改的地方 |
|---|---|
| 新增後端服務（Turso、Supabase Auth 等） | 這份文件補一段服務怎麼被保護；`.env.example` 補對應變數名稱 |
| 新增會存取使用者資料的功能 | `README.md` 的架構與安全性小節更新資料流程說明 |
| 修掉一個資安問題 | 上方「已知限制」移除對應項目，「更新紀錄」加一行 |
| 發現新風險但還沒修 | 誠實列進「已知限制」，不要略過不寫 |
